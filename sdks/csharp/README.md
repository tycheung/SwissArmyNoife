# C# SDK (`sak332`)

.NET 8 library `SwissArmyNoife.Sdk` — HTTP admin + MCP clients for SwissArmyNoife.

## Build / test

```bash
cd SwissArmyNoife/sdks/csharp
dotnet test
```

Scaffold only (`sak332-a`). Client surfaces land in `sak332-b` / `sak332-c`.

