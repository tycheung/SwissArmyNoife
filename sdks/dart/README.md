# Dart SDK (`sak338`)

Pub package `swissarmynoife` — HTTP admin + MCP clients for SwissArmyNoife.

## Test

```bash
cd SwissArmyNoife/sdks/dart
dart pub get
dart test
```

Scaffold only (`sak338-a`). Client surfaces land in `sak338-b` / `sak338-c`.
