# PHP SDK (`sak335`)

Composer package `swissarmynoife/sdk` — HTTP admin + MCP clients for SwissArmyNoife.

## Test

```bash
cd SwissArmyNoife/sdks/php
composer install
composer test
```

Scaffold only (`sak335-a`). Client surfaces land in `sak335-b` / `sak335-c`.
