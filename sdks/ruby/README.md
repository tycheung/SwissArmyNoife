# Ruby SDK (`sak333`)

Gem `swissarmynoife` — HTTP admin + MCP clients for SwissArmyNoife.

## Test

```bash
cd SwissArmyNoife/sdks/ruby
bundle install
bundle exec rake
```

Scaffold only (`sak333-a`). Client surfaces land in `sak333-b` / `sak333-c`.
