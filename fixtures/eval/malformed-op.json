{
  "schema": "sak.fixture.offer/v0",
  "offer": "eval.run",
  "source": "sak537-a",
  "bind_policy": {},
  "request": {
    "args": {
      "op": "explode",
      "checks": [{ "id": "n", "assert": "eq", "actual": 1, "expected": 1 }]
    }
  },
  "expect": { "status": "error", "code": "schema.invalid" }
}
