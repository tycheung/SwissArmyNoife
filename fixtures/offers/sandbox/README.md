# Sandbox goldens

Expected violation shapes for **filesystem jail** / argv cases, plus
`shell-wrapper-deny.json` (`sak554-b`) and `program-deny.json` (`sak554-c`).
Shared envelope: `sak.fixture.offer/v0` (see [`../README.md`](../README.md)).
