# LLM routing goldens

Hand-authored samples for **provider routing** (echo vs vault-backed providers).
Files use the shared `sak.fixture.offer/v0` envelope; see [`../README.md`](../README.md).

`echo-embed.json` is also the **sak571-d** offer-level embed golden (`provider` + `vectors`).
