# community.echo

Minimal **wasm** marketplace template for SwissArmyNoife Phase 4.

- Runtime: `wasm` (never `native`)
- Payload: `module.wat` — smoke export `add`
- Install: `swissarmynoife module install <path-to-this-dir>`

Publish to marketplace-api later; this tree is the offline/path source.
