[package]
name = "offer-sandbox"
description = "SwissArmyNoife sandbox.* offers (jail, backends, exec)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
control = { path = "../control" }
types = { path = "../types" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true }

[features]
default = []
# Wrap host NoneBackend exec with `unshare -n` (Linux). No effect on Windows.
sandbox-unshare = []

[lints]
workspace = true
